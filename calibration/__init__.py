"""
calibration package
====================

Empirical calibration extension for the Avellaneda-Stoikov market-making
simulator (see CALIBRATION_PHASE1.md).

NOTE: This package is being built incrementally, task by task, per the
approved Phase 1 roadmap. Only Task 2 (load_data.py / LOBSTERLoader) has
been implemented so far. The public convenience function `calibrate()`
described in the roadmap's section 5.7 will be added in a later task
(Phase 1E) once estimate_sigma.py and estimate_lambda.py exist. Importing
it now would be premature, so it is intentionally omitted.
"""
